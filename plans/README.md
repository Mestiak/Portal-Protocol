# Portal Protocol — Animation Improvement Plans

Generated by the `improve-animations` skill after a `review-animations` pass on the live frontend. Plan-only: no source was modified. Backups of all target files are at `backups/2026-08-18.motion-plans-pre/`.

## Findings (vetted, by leverage)

| # | Plan | Severity | Category | Location | Finding |
| --- | --- | --- | --- | --- | --- |
| 1 | `001-transition-all-to-named` | HIGH | Performance | `app.css:707`; `+page.svelte:5294-5295,6341-6342`; `ApiTracker.svelte:1309,1359,1534,1762,1859,1938,1956`; `StatsModal.svelte:1282,1494,1527,1550`; `AnalyticsDashboard.svelte:1125`; `FilterBar.svelte:420,519,562,615` | `transition: all` animates unintended properties off-GPU; the formatter flex container (`+page.svelte:6341-6342`) animates layout props (`flex`/`width`) |
| 2 | `002-reduced-motion-guard` | HIGH | Accessibility | `+page.svelte:5119,6291,6303,5195,5530,5534,5660,5664,6213,6266`; `ApiTracker.svelte:849,1184,1188`; `app.css:79,942,1134` | Svelte `scale`/`fly`/`fade` transitions + keyframe loops ignore `prefers-reduced-motion` (only `slide` + drag-overlay are guarded) |
| 3 | `003-sidebar-duration-budget` | MEDIUM | Easing & duration | `app.css:114` (`.app-container`), `app.css:282-285` (`.sidebar`) | 350ms transitions exceed the 300ms UI budget and animate layout props (`grid-template-columns`, `max-width`) |

## Recommended execution order

1. `001-transition-all-to-named` — BLOCK driver; fixes the off-GPU layout animation. Mechanical, low-risk.
2. `002-reduced-motion-guard` — BLOCK driver; the a11y gap. Add one baseline rule + wire the Svelte guards.
3. `003-sidebar-duration-budget` — MEDIUM; tighten 350→300ms after the above land.

No dependencies between plans 1 and 2. Plan 3 is independent.

## Status

| Plan | Status | Verified |
| --- | --- | --- |
| 001-transition-all-to-named | DONE | `npm run check` 0/0, `npm run build` ✓ |
| 002-reduced-motion-guard | DONE | `npm run check` 0/0, `npm run build` ✓ |
| 003-sidebar-duration-budget | DONE | `npm run check` 0/0, `npm run build` ✓ |

Executed 2026-08-18. Backups of all 6 edited files at `backups/2026-08-18.motion-plans-pre/`.
Final drift check: 0 `transition: all` in shipped `src/` (only `*.reconstructed` backup retains the old pattern, out of scope).

## Note

Motion baseline is otherwise strong: Svelte `scale` transitions use `start: 0.92` (no `scale(0)`), `.subfolder-menu` uses `transform-origin: top right` (trigger-anchored), press feedback is `scale(0.90–0.97)` on `:active`, and no `ease-in` on UI. These are NOT findings and should not be touched.
